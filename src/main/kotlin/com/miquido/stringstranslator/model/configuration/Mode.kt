package com.miquido.stringstranslator.model.configuration

enum class Mode {
    /**
     * Convert from strings files to spreadsheet
     */
    TO_SPREADSHEET,
    /**
     * Convert spreadsheet file to strings files
     */
    FROM_SPREADSHEET
}